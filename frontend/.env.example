# Frontend environment (Vite). Copy to frontend/.env for local overrides.
# Backend origin (no trailing slash). Defaults to http://localhost:8080 if unset.
VITE_API_ORIGIN=http://localhost:8080
# Optional explicit API base; defaults to ${VITE_API_ORIGIN}/api
# VITE_API_URL=http://localhost:8080/api
